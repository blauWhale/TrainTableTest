class PublicTransport {
    constructor(name, number, departure,operator,destination, category) {
        this.name = name;
        this.number = number;
        this.departure = departure;
        this.category = category;
    }
}
module.exports = PublicTransport